"""Labeled eval fixtures for the Outlet Profiler agent behaviour.

Three behaviours are scored (see harness.py):
  1. mission -> archetype interpretation   (absolute, hand-labeled)
  2. reasoning_mode tagging                (absolute, from the documented rule)
  3. hypothesis verdict                    (absolute anchors + differential)

These are hand-authored ground truth — deliberately NOT generated by running
the agent (that would be circular). Verdict cases without an explicit
`verdict` are scored differentially (agent must agree with an independent
reference re-computation over the graded frame); the anchor cases carry an
absolute `verdict` that must hold regardless of data.
"""

# ── 1. mission text -> expected archetype (weights_from_mission) ────────────
MISSION_CASES = [
    ("launch a new premium SKU in Delhi", "premium_launch"),
    ("trade up my outlets to high-end lines", "premium_launch"),
    ("push volume with a trade scheme", "volume_scheme"),
    ("run a discount promo to grow volume", "volume_scheme"),
    ("widen distribution and must-stock coverage", "distribution"),
    ("range expansion / assortment width", "distribution"),
    ("tell me the outlets to improve order frequency", "frequency"),
    ("which outlets should reorder more often", "frequency"),
    ("protect my loyal outlets from churn risk", "retention"),
    ("defend my best accounts", "retention"),
    ("win back lapsing outlets", "reactivation"),
    ("reactivate dormant, inactive outlets", "reactivation"),
    ("improve segmentation of all my outlets", "balanced"),
    ("just grade all my outlets overall", "balanced"),
    ("help me understand my outlets", "balanced"),  # no keyword -> default
]

# ── 2/3. hypothesis verdict + reasoning_mode ────────────────────────────────
# mode is absolute (documented rule: a matched keyword -> deterministic; no
# keyword -> reasoning; too-small scope -> deterministic+inconclusive).
# verdict is absolute only where the answer is forced (no-keyword / empty scope);
# otherwise scored differentially against the reference re-computation.
VERDICT_CASES = [
    {"company": "Anchor", "hypothesis": "my outlets have gone dormant",
     "scope": {}, "mode": "deterministic"},
    {"company": "Hamdard", "hypothesis": "these outlets are lapsing",
     "scope": {}, "mode": "deterministic"},
    {"company": "Anchor", "hypothesis": "these outlets are underperforming vs their peers",
     "scope": {}, "mode": "deterministic"},
    {"company": "Colgate (India)", "hypothesis": "these are my top performing outlets",
     "scope": {}, "mode": "deterministic"},
    # no keyword -> always inconclusive + reasoning
    {"company": "Anchor", "hypothesis": "something feels off with these outlets",
     "scope": {}, "verdict": "inconclusive", "mode": "reasoning"},
    {"company": "Anchor", "hypothesis": "what is the story with my outlets",
     "scope": {}, "verdict": "inconclusive", "mode": "reasoning"},
    # empty scope (n < 10) -> inconclusive + deterministic
    {"company": "Anchor", "hypothesis": "are these dormant",
     "scope": {"region": "__NOWHERE__"}, "verdict": "inconclusive", "mode": "deterministic"},
]

# pass gates (fraction correct required)
GATES = {"mission": 1.0, "reasoning_mode": 1.0, "verdict_absolute": 1.0, "verdict_differential": 1.0}
