# Slim serve deps — the grading/agent path only needs Polars + Pydantic +
# FastAPI. No torch/open_clip (SigLIP typing is offline/optional); httpx is used
# by the onboarding subprocess AND the completion callback. Pinned for
# reproducible builds (Definition-of-Done).
fastapi==0.138.2
uvicorn[standard]==0.49.0
starlette==1.3.1
polars==1.42.1
pydantic==2.13.4
httpx==0.28.1
